Converter is a Flask App the purpose of which
is to enable users to execute the PeterHiggs Lambda function
by entering a URL to a browser such as Chrome
API Gateway timeout is 30 sec but the PeterHiggs Lambda function will
continue to run until finished or until timeout at 5 min max time.
